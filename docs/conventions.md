### Project conventions

- Use a TypeScript union for closed values that exist only inside trusted code.
- Validate values at every API, form, and environment boundary.
- Keep product features isolated by module.
- Keep browser-only code out of React Server Components.
- Add integrations only when a product requirement justifies them.
